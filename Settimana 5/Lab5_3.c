#include <stdio.h>
#include <stdlib.h>

int main()
{
    int matrix[3][3], i, j, k, h, conta;


    // Inizializzo la matrice
    for (i=0; i < 3; i++)
        for (j=0; j < 3; j++) {
            printf("Inserisci dato coordinate (%d;%d): ",i,j);
            scanf("%d", &matrix[i][j]);
        }

    if (i > 0 && j > 0 && i < N_RIGHE-1 && j < N_COLONNE -1)
        if (mat[i-1][j-1] == 1 &&
            mat[i-1][j])


}
